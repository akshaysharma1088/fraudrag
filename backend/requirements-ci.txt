# FraudRAG CI Requirements
# Strategy: pin exact versions for stable packages, use compatible-release (~=)
# for LangChain ecosystem to avoid sub-package conflicts

# ── Web Framework ─────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.12
pydantic==2.9.2
pydantic-settings==2.5.2

# ── LangChain ecosystem — let pip resolve compatible sub-versions ─────────────
# Pinning all sub-packages to exact versions causes conflicts.
# These ranges are the tested-compatible set for 0.2.x:
langchain>=0.2.16,<0.3.0
langchain-community>=0.2.16,<0.3.0
langchain-openai>=0.1.25,<0.2.0
langchain-anthropic>=0.1.23,<0.2.0
langchain-core>=0.2.45,<0.3.0
openai>=1.51.0,<2.0.0
anthropic>=0.34.2,<1.0.0
tenacity>=8.1.0,<9.0.0

# ── Knowledge Graph ───────────────────────────────────────────
neo4j==5.25.0

# ── Document Processing ───────────────────────────────────────
pdfplumber==0.11.4
pypdf2==3.0.1
python-docx==1.1.2
pillow==10.4.0
pytesseract==0.3.13
pdf2image==1.17.0

# ── Data Processing ───────────────────────────────────────────
pandas==2.2.3
numpy>=1.26.4,<2.0.0
pyarrow==17.0.0

# ── AWS ───────────────────────────────────────────────────────
boto3==1.35.0
botocore==1.35.0

# ── Database ──────────────────────────────────────────────────
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0
psycopg2-binary==2.9.10
redis==5.1.0

# ── Security ──────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==43.0.1

# ── Monitoring ────────────────────────────────────────────────
structlog==24.4.0
prometheus-client==0.21.0
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0

# ── HTTP / Utilities ──────────────────────────────────────────
httpx==0.27.2
python-dotenv==1.0.1
rich==13.9.0
typer==0.12.5

# ── Testing ───────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
anyio==4.6.2
factory-boy==3.3.0
